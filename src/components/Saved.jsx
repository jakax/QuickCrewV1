import React from "react";
import { View, Text } from "react-native";

const Saved = () => {
  return (
    <View style={{ paddingVertical: 50, left: 10 }}>
      <Text>Saved Jobs work in progress</Text>
    </View>
  );
}

export default Saved;